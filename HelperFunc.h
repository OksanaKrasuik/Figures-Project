#pragma once

// common functions

//length of string
int length(char*);

// copying one string to another
void strCopy(char*, const char*);

// safe input of number <type double>
double GetDouble();

// comparing 2 strings
bool CompareStr(const char*, const char*); 